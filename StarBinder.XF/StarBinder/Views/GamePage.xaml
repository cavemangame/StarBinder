<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:StarBinder.Controls;assembly=StarBinder"
             x:Class="StarBinder.Views.GamePage">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <controls:GameControl Level="{Binding CurrentLevel}"
                          BackImage="{Binding BackImageSvg}"
                          Command="{Binding StarTapCommand}"/>
  </Grid>
</ContentPage>